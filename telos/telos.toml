[code]
globs = ["tictactoe/domain/board.py", "tictactoe/ui/cli.py"]

[tests]
globs = ["tests/*.py"]

[test]
cmd = "pytest -q --junitxml={report} -k '{filter}'"
report = "junit.xml"

[policy]
tdd = "strict"

[agents]
hosts = ["claude", "codex"]
