constraint CON-0001 in project architecture "The domain never imports an interface" {
  rule  "Modules under tictactoe/domain/ must not import tictactoe.ui, tkinter, argparse, or sys."
  check "python3 tools/check_layers.py"
}
